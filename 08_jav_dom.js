// let heading = document.querySelector("h2");
// heading.innerText= heading.innerText + " from Code with Smaran Rawal";
// console.log(heading.innerText);

let adds= document.querySelectorAll(".box");

let idx=1;
for (add of adds){
    add.innerText=`creating unique value ${idx}`;
    idx++;
}
// add[0].innerText="creating unique text";


// add[1].innerText="creating unique ";

// add[2].innerText="creating ";
